func printClock() {
    println("Temps d'exécution est à " + clock());
}

func fib(n) {
    if (n <= 1) return n;
    return fib(n - 2) + fib(n - 1);
}

let mut name = "Andy";
printClock();

name = "Bonjour";

for (let mut i = 0; i < 10; i = i + 1) {
    if (i != 0) println(name);
}



println(fib(10));
