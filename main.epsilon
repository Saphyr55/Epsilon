class Optional  {

    let mut content;

    method ifPresent(fn) {
        fn(null);
    }

}

let optional = Optional();
optional.content = 100;

optional.ifPresent(func (content) {
   println(content);
});



